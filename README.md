"# Arrays-in-JavaScript" 

# To run and view the code output
# Open the html (.html) files in vs code or editor of choice
# In vs code open with live server extension
# Once its open on the browser right click, then click ispect and open the console tab
# Open the javascript (.js) file in vs code which contain different array methods with on point explanation commented out
# Uncomment the codes you want to view in your browser console tab